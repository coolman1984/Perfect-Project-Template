# licenses/

Full license texts for every bundled component, referenced from
`THIRD_PARTY_NOTICES.md` (Constitution Part 30.2).

Populated at build time. A bundled component whose license text is missing is a
release-blocking omission — `architecture verify --release` checks for it.
