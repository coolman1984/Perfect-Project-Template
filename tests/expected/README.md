# Expected outputs

Approved outputs and evidence that golden tests compare against
(Constitution Part 24).

An expected file changes only when a metric definition changes — and that
requires a version bump and an approval record (Part 25.7). Updating an expected
file to make a failing test pass, without a corresponding approved definition
change, is how a broken formula becomes permanent.
