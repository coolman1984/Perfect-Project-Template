# Gemini project instructions

Read `00_START_HERE_AI_AGENT.md` first and execute its detected mode end to end.
Then follow `AGENTS.md`, `PROJECT_SKILL.md`, `UNIVERSAL_ENGINE_SKILL.md` and
`.ai/READ_FIRST.md` in that order.

If the user says only "complete the project", do not ask for technical
direction. Detect first-Windows commissioning versus copied-master adaptation,
ask only missing business-meaning or approval questions, run the required
tests, and return only the verified archive(s) required by that mode.

Never claim completion if the current environment cannot execute code, run
tests or create ZIP files.
