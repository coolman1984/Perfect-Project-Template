[tables]
raw = "raw.production"
clean = "clean.production"
history = "analytics.history_production"

[columns]
business = ["production_date", "line", "order_number", "model_code", "produced_qty", "defect_qty", "category"]
measures = ["produced_qty", "defect_qty"]
category = "category"

[sql]
clean = "clean.sql"
checks = "checks.sql"
metrics = "metrics.sql"
insights = "insights.sql"

[dashboard]
config = "dashboard.toml"
