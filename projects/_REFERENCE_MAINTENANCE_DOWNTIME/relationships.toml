# No relationships: Reference B has exactly one source. The array stays
# present and empty so factory/project_contract.py's loader (which requires
# the file to exist and parse) has something valid to read (Part 16).
relationships = []
