# ChatGPT / Work instructions

Open `00_START_HERE_AI_AGENT.md` first. It contains the complete autonomous
workflow for an uploaded master template plus Excel files. Follow its detected
mode, then read `AGENTS.md`, `PROJECT_SKILL.md`, `UNIVERSAL_ENGINE_SKILL.md` and
`.ai/READ_FIRST.md`.

Do not ask the user for architecture, packages, databases, ports, build tools
or configuration choices. Ask only missing business meaning or approvals. A
browser chat without file/code execution must report that capability blocker;
it must not return a mockup and call it the finished offline application.
