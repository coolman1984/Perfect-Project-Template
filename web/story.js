/**
 * Insight Story presentation mode (Constitution Part 22.11).
 *
 * A MANUAL previous/next slide deck generated from VERIFIED insight objects —
 * never from generated prose, and never from numbers computed in the browser.
 *
 * Slide order:
 *   1 executive result
 *   2 trend and comparison
 *   3 largest contributors / Pareto
 *   4 concentration or stability view
 *   5 actions, owners, risks, and the data-quality caveat
 *
 * Rules:
 *   - No autoplay by default.
 *   - Pause when the window loses focus.
 *   - Keyboard and touch controls are required.
 *   - Each slide shows one message, its evidence chart, the period and filter
 *     context, and the source/run reference.
 *   - One directional transition; preserve focus across slides.
 *   - Exporting or printing preserves the message without animation.
 */

export function openStory(insights) {
  throw new Error('Not implemented. Build slides from verified insight objects only.');
}
