# CLAUDE.md

Claude entry pointer (Constitution Part 20.2). This file deliberately contains
**no rules of its own** — forking the rules across entry files is how projects
drift.

👉 **Read [`00_START_HERE_AI_AGENT.md`](00_START_HERE_AI_AGENT.md) first, then
[`AGENTS.md`](AGENTS.md) and [`PROJECT_SKILL.md`](PROJECT_SKILL.md).**

Quick start for this session:

```bash
./project_tool.sh doctor                                    # Linux/macOS
PROJECT_TOOL.bat doctor                                     # Windows
./project_tool.sh map context --task "<your task>" --budget 4000
```

Then read `.ai/CONTEXT_PACK.md` and `.ai/CURRENT_STATE.md` — and only the files
they name. Do not scan the repository (Part 0.1).
