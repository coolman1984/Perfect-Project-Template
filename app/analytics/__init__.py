"""Layer 8: trusted calculation and evidence-backed insight (Part 10).

All trusted arithmetic lives in versioned SQL. Python orchestrates,
validates, converts and formats; it never holds a second KPI formula.
"""
